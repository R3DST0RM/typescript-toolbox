/**
 * This methods does literally nothing, on purpose.
 * @param args - All params won't be used at all.
 * @returns {void} - Nothing to return as this method does nothing.
 */
export const doNothing = (...args: unknown[]): void => {
    /* void */
};
